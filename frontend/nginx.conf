server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        
        # Dòng quan trọng nhất: Nếu không tìm thấy file, quay về index.html
        try_files $uri $uri/ /index.html;
    }
}